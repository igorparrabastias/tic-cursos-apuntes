
*~
.aider*
*.bak
book/
/build/
.cache/
*.csv
/data/
dev-problems/
/dist/
.env
env/
# Excluir archivos de configuración específicos del entorno
# Excluir archivos de log
# Excluir archivos de salida de Jupyter Notebooks
# Excluir directorios de configuración del entorno de desarrollo
# Excluir directorios de datos o archivos grandes que no deberían ser parte del repositorio
# Excluir directorios que suelen contener archivos binarios o de otro tipo no relacionados directamente con el código fuente
# Excluir posible archivo de configuración del entorno virtual de Python
# Excluir todos los archivos temporales y directorios
/.idea/
*/.ipynb_checkpoints/*
.ipynb_checkpoints/
*.log
/node_modules/
# NOTA: No excluimos explícitamente archivos .md y .ipynb ya que queremos incluirlos en el repositorio
*.pyc
/__pycache__/
# _revision/ ahora SÍ se versiona: tubería de build del doc PLN/IA
# (out/*.md fuente, assemble.js, gen-headers.js). Ver HANDOFF.md.
# Sin secretos: gen-headers.js lee OPENAI_API_KEY de process.env.
.secret
_site/
site/
*.sublime-workspace
*.swp
*.temp
*.tmp
tools/
*.venv
venv/
/.vscode/
*.xlsx

# notebooks copiados por nb_hook.py durante el build (la fuente real es notebook/)
pages/notebook/
